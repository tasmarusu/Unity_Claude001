# プロジェクト運用ルール

## Git push の自動実行について

- コミットを作成した後、**確認なしで自動的に `git push` してよい**。
- ただし以下は対象外とし、通常通り確認を取ること。
  - `master`(main)ブランチへの直接push
  - force push(`--push --force` 等)
  - リベースや履歴書き換えを伴うpush
- 上記以外の、作業ブランチ(例: `claude/*`)への通常のpushは、都度の確認を省略してよい。

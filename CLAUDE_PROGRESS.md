# CLAUDE_PROGRESS.md (ゲーム開発セッションの進捗・再開用)

最終更新: 2026-09-26(追加開発: 因果の可視化+都市の生命感) / 担当: ゲーム開発セッション

## 今作っているもの
「一撃ビル解体」に **1(Smash Fest型: 狙いと制約のあるステージ)** と **2(Mob Control型: 階ごとの倍率ゲート)** を統合した試作。
(根拠: 調査レポート「ハイカジ売れ筋100本と模倣候補」の候補1+2。ワンタップ操作は維持。)

## ゲームループ
PLAY → 狙う(押している間、狙った階と巻き込まれる階がハイライトされ、予測スコアが指の上に出る) → 離して崩す →
ゲート通過で倍率(×2/×3/÷2) → リザルト(星1〜3・スコア・建築史解放) → NEXT/RETRY(1タップ)。
- ステージは番号から決定的に生成(StageGenerator)。最適解を総当たりで求め、星は最適の60%/90%。
- ショットは1→2→3発へ増加。難易度は階数・ゲート数・悪ゲートが段階的に増える。
- 動画広告視聴で同ステージを+1ショットでリトライ(リワード)。

## 主要ファイル(Assets/Scripts)
新規: StageSpec.cs(FloorKind/StageSpec/ScoreRules) / StageGenerator.cs / FloorBadge.cs / StageHudUI.cs / StageResultUI.cs / UiKit.cs / UiSprites.cs / SafeAreaUtil.cs
書き換え: GameManager.cs(状態機械 Ready→Playing→Resolving→Result) / BuildingTower.cs / Floor.cs / TapDemolishController.cs(押して狙う→離して撃つ) / ScoreManager.cs
追加: ProceduralAudio(ゲート/失敗/星/ファンファーレ/クリック) / JuiceManager(PlayGate等) / ScoreFeedbackUI(ゲートバナー)
削除: RewardBonusUI.cs(リザルト内の動画ボタンに統合。シーンのRewardBonusRootも削除)
設定: ProjectSettings 画面向きをPortrait固定。

## 広告
既存のGoogle Mobile Ads(テスト広告ID)を継続使用。バナーは下部固定で、読み込み後に3Dカメラのviewportをバナー高さ分切り上げ、
UIは SafeAreaFitter(Safe Area+バナー高さ)の内側に配置。初期化失敗でもゲームは止まらない。インタースティシャルはステージ遷移3回ごと。
※ネイティブバナーはEditorでは疑似表示のみ。実機での見え方は未確認。

## アセット
- Assets/Resources/BuildingHistory/ : 建築史30+強化3アイコン(納品済み)
- Assets/Resources/StageUI/star.png : 星アイコン(モデル制作が納品。UiSprites.Starが自動で優先使用)
- 注意: Assets/UI/BuildingHistory/ に旧パスの複製アイコン(未追跡・別GUID)が再出現している。コミットしていない。モデル制作側で整理が必要。

## 確認済み(Editor Playモード、MCP経由)
コンパイルエラーなし / ステージ1〜3の生成と難易度カーブ / 成功→リザルト→NEXT / 失敗→リトライ / 複数ショット継続 /
Safe Area計算(各アスペクト比の疑似値)/ HUD・ゲート看板・リザルトの表示崩れなし。

## 追加開発(2026-09-26): 「壊す→ポイント→ゲージ→MAXでクリア」の因果を見せる + 生きた都市
ユーザー指示: 説明文ではなく、触るだけで原因→結果が分かること。
- 1階ごとに「ヒビ(揺れ+白フラッシュ+小さな粉塵)→崩壊→ポップ」の段階演出(chain間隔を約0.17→0.10秒。全体を遅くせず認識できる速度)。
- 壊した階から「+N」がポップ→スコアへ飛ぶ→着いた瞬間にスコア表示・ゲージが増える(ScoreFeedbackUI.PopupRoutine)。
  実スコア(CurrentScore)と表示スコア(DisplayedScore)を分離。ゲージ/☆判定は表示スコア連動。着地音は連続で音階が上がる。
- ポイントゲージ「現在/MAX」(StageHudUI)。MAX=最適解の60%(ゲージ目盛りにMAX表示)。MAXでフラッシュ+「MAX!」→結果は「CLEAR!」。
  ショット切れでMAX未達=失敗(「あとNptでMAX」)。KEEP階を壊す=失敗(スコアは無効表示)。狙い中はゲージ上にゴースト予測と「MAX!」予告。
- ☆は3条件(1ステージ各1個、リザルトの☆=取得数): (1)金色に脈動するボーナス階(得点×2)を壊す (2)1発で連鎖5(HUDの☆5アイコンが予兆で脈動)
  (3)ゲージ右端の☆マークまで伸ばす(最適の90%)。条件を満たした場所から☆が弾け出て☆カウンターへ飛ぶ。ステージ1は3条件が自然に全部体験できる。
- 得点基準を10→100に。建物ごとの価値差はボーナス階(×2)とゲートで表現(全階が同点ではない)。
- 背景: CityLife(自己起動)。CityBackdrop(静的218棟)を毎回組み直し(外壁の近傍重複回避・高さゾーン別のばらつき・低層/超高層の混在。
  外壁別に結合メッシュ化=描画数を削減)。道路沿いに街路樹/街灯(結合1メッシュ)。車(左側通行、車種/色/速度バラバラ、車間維持、横断歩道手前でUターン周回、
  最大7台プール)と歩行者(路肩→横断歩道を渡る、最大8人プール、主役のタワー前には出ない)。すべてCollider/Animatorなし。
  素材はモデル制作納品(a191c55: Resources/CityLife/People,Cars,Props、Mat_CityLife、ToonLitProp)。Roadオブジェクトの位置から道路寸法を読む。
- 検証: Editor Play(MCP)でステージ1成功/ステージ3失敗/背景生成(結合メッシュ10枚、アクティブRenderer 274→60)を確認。
  MCPのUnity接続が一時切断されたため、Unity同梱Roslynでの単体コンパイル確認(scratchpad/cc.sh方式)も併用した。
- 未確認/課題: 実機での車・歩行者の見え方と負荷 / 背景を毎回変えたことでタワーの視認性が下がる場面がないか(白いタワー×明るい背景)を要プレイ確認 /
  ステージ間のMAX点数の増減がやや不規則(ゲート積による)/ 建築物の種別(オフィス/商業/マンション)は外壁マテリアル単位でしか区別できていない。

## ユーザーフィードバック対応(2026-09-26 午後)
- 「KEEPいる?」→ KEEP(保護階)を全廃。生成・スコア・失敗理由・看板・NGバナーから削除(失敗はショット切れでMAX未達のみ)。
  判断の面白さは悪ゲート(÷2)で作る(最適解の一手目が一番下にならない配置を生成時に保証)。
- 「ステージ9の☆3が分からない」→ 3つ目の☆(ゲージ端の☆)を最適の90%→75%(=MAXの1.25倍)に緩和。
  さらに「狙っている間に、その一撃で取れる☆」を指の上のラベル下に☆アイコンで予告し、☆カウンターの次の空き枠を脈動させる
  (ボーナス階を巻き込む/連鎖5以上/☆マークまで届く、のいずれか)。
- 「ビルの☆の役割が分からない」→ 上記の予告で「この階を含めて撃つと☆が1つ入る」が押す前に分かる。壊すと☆が弾けてカウンターへ飛ぶ。
- 「右上と左上のUIいらない」→ ミュートボタン(MuteToggleUI削除)とBESTプレート/BEST文字(シーンから削除)を撤去。BESTは内部記録のみ(リザルトのNEW BEST!)。
- 「ビルをもっとタッチしやすく」→ TapDemolishControllerで、レイキャストが外れても画面上で最も近い階に吸着(左右は画面幅の約14%、階間の隙間、
  頭上/足元は画面高の約9%まで拾う)。押している間もその階がハイライトされる。パラメータはInspectorで調整可。
- 「背景の人や車を手前にも」→ CityLifeにFrontInfo(CrossRoad/Crosswalk_Front/広場の寸法を読む)を追加。手前の広場をタワーの前で横切る歩行者、
  手前の横断歩道を渡ってくる歩行者、画面下を左から右へゆっくり通る車を追加(奥と同じプール。車は最大3台追加、人は最大5人追加)。

## タッチ改善と☆3シミュレーション(2026-09-26 夜)
- ステージは手続き生成で無制限(ステージ番号から決定的に生成、毎回ランダムではない。リトライは同じ配置。ビルの見た目は10種を順番に巡回)。
- 生成時に「☆3(ボーナス階+1発で連鎖5+ゲージ☆マーク)を全部取れる撃ち方」が存在するかを総当たりで確認し、無ければ別配置に引き直す
  (ScoreRules.AllStarsFeasible / StageGenerator.Generate)。修正前は300ステージ中約69(23%)が☆3不可能だった。
  修正後は、ステージ1〜200 × 履歴ボーナス31段階(0〜+60%) = 6200通りで☆3不可能ゼロ。
- 最大階数を12→10に(階が画面上で小さすぎてタッチしにくいため)。
- タッチ: ①判定位置を指先の分だけ少し上へ(画面高の1.2%、タッチのみ) ②狙いのヒステリシス(隣の階への切替をやや遅らせる)
  ③押した階が画面上130px未満(背の高いビル)のときは、押した階を起点に「指130px=1階分」で動く精密モード ④狙った階の下端に切断ラインを描画
  (ビルより左右にはみ出すので指で隠れても分かる) ⑤看板を階の高さに収めて、隣の階に見えないように。

## タッチ位置ずれの再修正・カメラ・効果音(2026-09-26 夜その2)
- 原因: 前回入れた「指先オフセット」と「精密モード(相対移動)」が、指の真下の階とハイライトのずれを生んだ。両方廃止して指の真下を直接狙う方式に戻した。
  加えて、指を離した瞬間のタッチ座標は指が転がってずれやすいので、離した瞬間は再判定せず、直前まで狙っていた階を撃つ
  (押してすぐ離した場合だけ離した位置で拾う)。
- ビルを大きく: 階を横に25%広げ(BuildingTower.WidthScale)、カメラをFOV変更ではなく「寄せる/離す」方式に(CameraFraming)。
  タワーの高さが画面の約60%になる距離(最短16)へ。階1つの見た目の高さ(正面)は10階のタワーで約118px(以前は約90px)。
  画面揺れはCameraFraming.SetShake経由(基準位置を上書きしない)。看板は階の正面に貼り付け(階から浮いて見えない)。
- 効果音: これまでは全てコード合成(ProceduralAudio)。今回、Kenney.nl の CC0 効果音(Impact Sounds / Interface Sounds、計23ファイル約200KB)を
  Assets/Resources/Sfx に導入し、グループ名_番号(tap_hit/break/crack/tick/gate/star/fanfare/fail/click)で JuiceManager が優先再生。
  ファイルが無いグループは従来の合成音にフォールバック。ライセンス文は docs/third_party/ (CC0のため表記義務なし、出典記録として保存)。

## ☆ルールの作り直しと実エンジン検証(2026-09-26 深夜)
ユーザー指摘: 「MAXに届くと何がもらえるの?」「3つ目の☆の条件が分からない」「Stage12で☆3を本当に取れたか?」「MAXを超えても続けさせて」
- ☆の3条件を、すべて画面上に印がある3つに統一: ①ゲージ上のMAXの位置の☆(届くと☆) ②ゲージ右端の☆(最適の75%に届くと☆) ③建物のボーナス階の☆(壊すと☆)。
  連鎖5の☆は廃止(コンボは得点倍率としてのみ残り、HUDは「COMBO n」表示だけ)。
- MAXに届いてもステージは終わらない: ショットと階が残り、☆が残っている間は続けられる。撃ち切る/壊し切る/☆を全部取ると終了。
  MAX後にショットが残っているときは画面下に FINISH ボタンが出て、そこで終えられる(MAX未達では終えられない)。MAX未達で終了すると失敗。
- 検証の反省: 前回の「☆3が取れる」確認は、自分で書いたソルバー上の理屈だけで、実際のゲームで撃って確かめていなかった
  (しかもソルバーは「MAXでステージが終わる」前提のまま実ルールと食い違っていた)。
  そこで QA用 StageAutoPlayer(#if UNITY_EDITOR || DEVELOPMENT_BUILD)を追加。☆3の撃ち方をソルバーで探し、その手順を実際のゲーム
  (GameManager/BuildingTower/ポイント飛翔/☆判定/ステージ終了条件)でそのまま実行して、リザルトの☆が本当に3つかを確かめる。
  使い方: Play中に execute_code で `OneTapDemolition.StageAutoPlayer.Begin(1, 300, 5f)` → 終了後 `StageAutoPlayer.Report` を読む。
  Unityウィンドウがフォーカス外だとPlayのフレームが止まるので、実行中だけ `Application.runInBackground = true` にして、終わったら false に戻す。
  進行度(PlayerPrefs)は実行後に元へ戻る。
  結果: ステージ12は撃ち方[6,0]で実際に☆3(スコア4714、☆マーク3550)。ステージ1〜300を全て実プレイして300/300で☆3。
- **今後ステージ生成/☆/クリア条件を変更したときは、コミット前に必ず StageAutoPlayer を実行して全ステージ☆3を確認すること。**

## ゲージの母数・上部スコア廃止・クリア演出(2026-09-26 深夜その2)
- ゲージの母数(右端)を「そのステージの最大得点(OptimalScore)」に変更。「現在値 / 最大得点」表示で、クリアの印(60%)と右側の☆(75%)はゲージの途中に置く。
  クリア確定の表示は MAX! → CLEAR!(判定ロジックは変更なし。リザルトも 現在値 / 最大得点、失敗時は「あと N ptでクリア」)。
- 上端の大きなスコア数字(ScoreCanvas の ScoreText/ScoreBackdrop)は非表示(ScoreUI が起動時に隠す)。HUDを上端へ移動し、「+N」はゲージの塗りの先頭へ飛ぶ(StageHudUI.ScoreLandingPosition)。
- クリア演出(CityCelebration、成功時のみ・☆3はより強く・次ステージ開始で止まる): 歩行者はぴょんぴょん跳ねて体をひねり、車はホップして傾く、
  背景ビルは1棟ずつ「チンアナゴ」(地面からにゅーっと伸縮して首をふる、位置ごとに位相をずらした波)、街路樹はぼよんと伸縮。
  ビルは通常は結合メッシュ(軽い)で、お祝い中だけ個別メッシュ版(CityBackdropEels)に切り替える。

## 仕様適合の全数確認(StageAudit)と自動終了の廃止(2026-09-26 深夜その3)
- ユーザー指摘: 「PERFECTなのに 8640 / 9152 で満点ではない。今まで仕様通りの挙動か、全部確認して」。
- 新QAツール StageAudit(エディタ/開発ビルド専用): 実エンジンでランダムな撃ち方と最高得点の撃ち方を遊び、各ショット後のスコア・ステージ終了タイミング・
  最終の☆/失敗/スコアが、ScoreRules から計算した期待値と一致するかを確認する(Begin(from,to,timeScale,ランダム本数))。
- 結果: ルール(スコア・☆・失敗判定・終了)は実エンジンと一致(食い違い0)。ただし、☆を3つ取り切るとステージが自動終了する仕様のため、
  ゲージの母数を「最大得点」にした後は、最高の撃ち方でも最大得点に届かないステージが60中52あった(例: Stage15 は 8640点で終了、最大 9152)。
- 修正: ☆を3つ取り切っても自動終了しない(GameManager.OnChainFinished)。ショットと階が残る限り続けられ、撃ち切る/壊し切る/FINISHで終わる。
  最高の撃ち方は必ずゲージ満点(最大得点)に届く。PERFECT は「☆3つ」の意味で、満点である必要はない(75%以上+ボーナス階)。
- 再検証: StageAudit 1〜150 × (最高得点1+ランダム3) = 600プレイで食い違い0・最高得点の撃ち方は150/150でゲージ満点かつ☆3。
  StageAutoPlayer(ソルバー計画/1発)は300/300で☆3。

- FULL SCORE!: そのステージの最大得点(ゲージ満タン)に届いたときだけ表示。プレイ中はゲージが満タンになった瞬間にバナー、
  リザルトは金色の「FULL SCORE!」(PERFECT! = ☆3つ、CLEAR! = クリア、の上位)。StageResultUI / StageHudUI。

- リワード広告のヒント: プレイ中、画面左下の「▶ ヒント」を押して広告を最後まで見ると、今の残りショット・残りの階から最大得点になる一手を
  緑の線(狙う階の下端)と「ここ! 満点コース / 最大 N」のラベルで示す。次のショットを撃つと消え、次の一手はまたヒントを見られる
  (StageHudUI.GrantHint、内部は ScoreRules.BestScore の総当たり)。エディタでは広告SDKが動かないため AdsManager.IsRewardedReady を常に true にして
  流れを確認できる(実機は広告が読み込まれているときだけボタンが出る)。
- Stage15(履歴+60%)の満点(9152)の撃ち方: 下から5つ目→下から4つ目→一番下(8640+80+432)。

## 未確認・残課題
- 実マウス/タッチでの「押して狙う→離して撃つ」入力の実操作確認。MCPからは入力を注入できないため、コントローラのUpdateAim/Fireを直接呼んで確認済み(狙った階の判定・予測スコア=実スコア・空撃ちはショット消費なし)。実際の押下/離し・UI上のタップ無視・指の下の予測ラベル位置は未確認
- 注意: タワー生成と同じフレームではコライダーがまだ物理に反映されず狙えない(実プレイでは問題なし)
- 実機(Android)でのバナー領域・ノッチ・各アスペクト比の見え方
- 保護階を壊した失敗時のスコア表示(失敗でもスコアは出る。ベストには記録しない)
- ドロップゾーン(Smash Fest型の落下目標)は未実装。次の拡張候補。
- 星3の閾値(最適の90%)がやや厳しい可能性。プレイテストで調整。
- 進行度(PlayerPrefs OneTapDemolition_StageIndex)を1へ戻したい場合はエディタで削除。

## クリア音とStage25の☆問題(2026-09-26 深夜)
- クリア音: 鐘(合成)をやめ、OpenGameArt CC0のファンファーレに差し替え。☆3=perfect_0(約10秒のオーケストラ)、それ以外のクリア=clear_0(短いジングル)。
  リザルト開始時に鳴らし、次ステージ開始で0.25秒フェードして止める(JuiceManager.PlayFanfare/StopFanfare)。ライセンスは docs/third_party/OpenGameArt_Fanfare_Licenses.txt。
- ユーザー指摘「Stage25は下から4つ目を壊して4000点超えでも☆2つ」の原因: ☆マークを「最適解(複数ショットの合計)の75%」で決めていたため、
  ボーナス階を巻き込む素直な1発ではマークに届かない配置があった(履歴ボーナス+40%のStage25は3976点<マーク4350点。MAX☆とボーナス☆の2つ)。
  全ステージ調査: ボーナス階を巻き込む1発の最高スコア < ☆マーク のステージが 300中123(複数ショットのステージ298中123)、
  最高得点の1発がボーナス階を外すステージが98あった。
- 修正: 生成時の条件に ScoreRules.OneTapCanEarnAllStars を追加(ボーナス階を巻き込む1発の最高スコア ≥ ☆マーク。1ショットのステージは、最高得点の1発自体がボーナス階を巻き込むこと)。
  満たさない配置は引き直す。結果は履歴ボーナス0/+20/+40/+60/+100%の全てで、ステージ1〜300が条件を満たす(不公平ゼロ、☆3不可能ゼロ)。
- 実エンジン検証(StageAutoPlayer): 従来のソルバー計画モードで300/300、新設の oneTap モード(ボーナス階を巻き込む1発の最高スコアだけを撃つ)でも300/300で☆3。
  今後のステージ変更では両モード(Begin(1,300,6f,false) と Begin(1,300,6f,true))を確認すること。

## 次の作業開始地点
1. 実機/エディタで押して離す入力を実際に触って感触確認(長押し遅延・指の下の予測ラベル位置)
2. 星閾値・ステージ難易度の微調整 3. ドロップゾーン等の追加は方針確認後
